use std::ops::Mul;
use std::ops::Add;
use std::ops::Sub;
use std::ops::Div;
use std::cmp::PartialEq;
use num_rational::Ratio;
use num_traits::Num;
use crate::matrix::*;
use crate::matrix::constructors::Constructors;


